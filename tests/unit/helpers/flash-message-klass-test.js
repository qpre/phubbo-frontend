import {
  flashMessageKlass
} from '../../../helpers/flash-message-klass';
import { module, test } from 'qunit';

module('FlashMessageKlassHelper');

// Replace this with your real tests.
test('it works', function (assert) {
  let result = flashMessageKlass(42);
  assert.ok(result);
});
