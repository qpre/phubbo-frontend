dependencies:
  pre:
    - npm install -g bower
    - npm install -g ember-cli
  override:
    - npm i
    - bower i
    - sudo pip install awscli

deployment:
  production:
    aws:
    branch: master
    commands:
      - ember build
      - ./deploy.sh
