machine:
  node:
    version: 5.4.0

dependencies:
  override:
    - npm install
    - npm install -g s3-upload

test:
  override:
    - pwd

deployment:
  production:
    branch: feature/refactor-redux
    commands:
      - npm run dist
      - npm run deploy
