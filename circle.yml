dependencies:
  pre:
    - npm install -g bower
    - npm install -g ember-cli
  override:
    - npm i
    - bower i

deployment:
  staging:
    aws:
    branch: master
      commands:
        - ember build
        - /bin/bash deploy.sh
