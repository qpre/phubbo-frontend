dependencies:
  pre:
    - npm install -g bower
    - npm install -g ember-cli
  override:
    - npm i
    - bower i

deployment:
  production:
    branch: master
    commands:
      - ember build
      - ./deploy.sh
